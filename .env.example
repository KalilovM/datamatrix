# =============================================================================
# Environment Variables Template
# =============================================================================
# Copy to .env.local for development or /etc/datamatrix/.env for production
# See .env.local.example for development and .env.prod.example for production
# =============================================================================

# Core
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/datamatrix_dev?schema=public"

# NextAuth - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# Session - Generate with: openssl rand -base64 32
SESSION_SECRET=your-session-secret-here

# Application URLs
NEXT_API_URL=http://localhost:3000
APP_DOMAIN=localhost

# SSL (production only)
SSL_EMAIL=admin@yourdomain.com

# =============================================================================
# NEXT_PUBLIC_* Variables (Exposed to Browser - DO NOT put secrets here!)
# =============================================================================
NEXT_PUBLIC_APP_NAME=DataMatrix
NEXT_PUBLIC_API_URL=http://localhost:3000
